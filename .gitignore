# transient clones and tool caches — never commit these
work/
work_hugo/
.gocache/
*.tmp
node_modules/

# transient batch run logs
results*_run.log
results_run.log
*_run.log

# temporary diagnostics
.diag*.txt

# census pipeline intermediates (resume-truth is committed manifest.csv)
census/parts/
census/manifest_parts/
census/data/
census/.batch.*
census/drive.log
census/.cache/

# census2 pipeline intermediates
census2/parts/
census2/manifest_parts/
census2/data/
census2/.batch.*
census2/drive.log
census2/.cache/
